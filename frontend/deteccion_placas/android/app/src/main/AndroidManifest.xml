<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISOS DE LA APLICACIÓN (AGREGADOS) -->

    <!-- 1. Red: Necesario para todas las conexiones HTTP y HTTPS (ngrok) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 2. Cámara: Necesario para capturar imágenes con la cámara -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- 3. Ubicación/GPS: Se requieren ambos para obtener la ubicación precisa -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- 4. Almacenamiento: Limitado a versiones antiguas (maxSdkVersion="32") para evitar conflictos en APIs recientes. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <!-- AJUSTE DE SEGURIDAD DE RED: Apunta al archivo network_security_config.xml -->
    <application
        android:label="deteccion_placas"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Bloque CRÍTICO: Permite consultar si hay aplicaciones que manejen esquemas específicos.
         Necesario para canLaunchUrl en Android 11+ (API 30+). -->
    <queries>
        <!-- 1. Permite consultar si hay navegadores que puedan manejar HTTPS -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!-- 2. Permite consultar si hay apps de mapas (esquema geo:) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>

        <!-- 3. Permite consultar específicamente por el dominio de Google Maps -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" android:host="maps.google.com" />
        </intent>
    </queries>
</manifest>